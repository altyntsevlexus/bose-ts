export type ProductType = {
  title: string;
  description: string;
  price: number;
  colors: string[];
  images: string[];
  productId: string;
};

export type CategoryType = {
  title: string;
  image: string;
  link: string;
};
